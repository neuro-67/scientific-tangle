# Base URL of the backend API. In local dev the Vite proxy forwards /api
# to the FastAPI server, so the default relative "/api" works out of the box.
VITE_API_BASE_URL=/api

# Optional: override the dev proxy target (defaults to http://localhost:8000).
# VITE_PROXY_TARGET=http://localhost:8000
