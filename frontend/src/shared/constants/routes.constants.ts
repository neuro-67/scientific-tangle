/** App-wide route paths. Import these instead of hardcoding URL strings. */
export const ROUTES = {
  login: "/",
  search: "/search",
  answer: "/answer",
  dashboard: "/dashboard",
  upload: "/upload",
  history: "/history",
  notFound: "*",
} as const;
